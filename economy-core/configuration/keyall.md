# KeyAll (`economy/keyall/`)

A periodic reward command that dispatches to every online player.

## `economy/keyall/config.yml`

```yaml
keyall:
  interval-minutes: 60
  command: "keyall give %player% key 1"
```

